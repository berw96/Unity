#define INITAILIZE_OBJECT_TRANSFORMS
#if (UNITY_2019_3_OR_NEWER && INITAILIZE_OBJECT_TRANSFORMS)

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static InitialObjectTransform.InitialObjectTransformData;

public class InitializeObjectTransforms : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
#endif